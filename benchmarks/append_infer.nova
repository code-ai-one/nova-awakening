// 测试: `定义 M=[]; 追加(M,0.5)` 是否能让 M[0] 识别为浮点元素
函数 主程序() {
    定义 可变 M = []
    追加(M, 0.5)
    // 若 M 标 __浮点列表__: sum = 0.5 + 1.0 = 1.5 bits = 4609434218613702656
    // 若未标记: M[0] 作整数 = 4602678819172646912 → cvtsi2sd → + 1.0 ≈ 4.6e18
    定义 sum = M[0] + 1.0
    打印(字符串拼接("sum bits=", 转字符串(sum)))
    返回 0
}
