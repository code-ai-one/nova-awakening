函数 主程序() {
    // 直接打印大整数
    定义 a = 4620693217682128896
    打印(字符串拼接("bignum=", 转字符串(a)))
    
    // 负数
    定义 b = 0 - 1
    打印(字符串拼接("-1=", 转字符串(b)))
    
    // 接近 7.0 IEEE bits 的数
    定义 c = 4620693217682128896
    打印(字符串拼接("7.0 IEEE=", 转字符串(c)))
    返回 0
}
