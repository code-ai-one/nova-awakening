函数 模糊或(a, b) {
    定义 _a = a + 0.0
    定义 _b = b + 0.0
    返回 _a + _b - _a * _b
}

函数 主程序() {
    定义 r = 模糊或(0.3, 0.7)
    打印(字符串拼接("r bits=", 转字符串(r)))
    // 期望 0.79 = 0x3FE947AE147AE148
    返回 0
}
