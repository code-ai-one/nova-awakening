// 纯打印字面量, 不加法
函数 主程序() {
    定义 a = 7.0
    打印(字符串拼接("7.0 bits=", 转字符串(a)))
    // 期望 7.0 bits = 4620693217682128896
    
    定义 b = 6.5
    打印(字符串拼接("6.5 bits=", 转字符串(b)))
    // 期望 6.5 bits = 4619567317775286272
    返回 0
}
