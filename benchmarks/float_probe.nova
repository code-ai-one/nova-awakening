// 探测: Nova 浮点支持
函数 主程序() {
    定义 a = 3.14
    定义 b = 2.71
    定义 c = a + b
    定义 d = a * b
    打印(字符串拼接("a+b=", 转字符串(c)))
    打印(字符串拼接("a*b=", 转字符串(d)))
    // 简单比较
    如果 (a > b) { 打印("✓ 3.14>2.71") }
    // 负数
    定义 neg = 0 - 1.5
    打印(字符串拼接("-1.5=", 转字符串(neg)))
    返回 0
}
