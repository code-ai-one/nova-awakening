// 测试: `定义 r = f(浮点列表)` 是否让 r 识别为浮点列表
函数 identity_list(M) {
    返回 M
}

函数 主程序() {
    定义 可变 M = []
    追加(M, 0.5)
    // r 调用返回, 参数 M 是 __浮点列表__ → r 标 __浮点列表__
    定义 r = identity_list(M)
    // sum = r[0] + 1.0 = 0.5+1.0 = 1.5 若规则生效
    定义 sum = r[0] + 1.0
    打印(字符串拼接("sum bits=", 转字符串(sum)))
    返回 0
}
